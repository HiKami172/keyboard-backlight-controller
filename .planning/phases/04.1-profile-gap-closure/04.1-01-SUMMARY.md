---
phase: 04.1-profile-gap-closure
plan: 01
subsystem: ui
tags: [gtk4, adwaita, application, autostart, tray]

# Dependency graph
requires:
  - phase: 04-system-tray-and-autostart
    provides: Application._on_activate with _tray_only flag and _restore_last_profile method
provides:
  - _restore_last_profile() called unconditionally before _tray_only guard (PROF-04 fix)
affects: [autostart, tray-only mode, hardware restore on startup]

# Tech tracking
tech-stack:
  added: []
  patterns: [call restore before conditional branching to ensure both code paths get the side effect]

key-files:
  created: []
  modified:
    - kbd_backlight/ui/application.py

key-decisions:
  - "_restore_last_profile() moved before if not self._tray_only: — one call serves both code paths without duplication"
  - "Comment 'PROF-04: always restore on first activation' added inline to make intent clear for future readers"

patterns-established:
  - "PROF-04 pattern: unconditional restore before conditional branching — restore-then-branch, not branch-then-restore"

requirements-completed: [PROF-04]

# Metrics
duration: 2min
completed: 2026-02-23
---

# Phase 4.1 Plan 01: Profile Gap Closure — PROF-04 Fix Summary

**One-line move of `_restore_last_profile()` before `_tray_only` guard so keyboard hardware restores last profile on every autostart, not only on interactive launches**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-02-23T01:53:01Z
- **Completed:** 2026-02-23T01:55:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Closed PROF-04 gap: launching via XDG autostart (`--tray-only`) now restores the user's last-used profile to hardware instead of leaving the keyboard at firmware default
- `_restore_last_profile()` now runs unconditionally on first activation for all launch modes (interactive and tray-only)
- All 72 existing tests pass — hardware and profile data layers unaffected by the application.py change

## Task Commits

Each task was committed atomically:

1. **Task 1: Move _restore_last_profile() before _tray_only conditional** - `d2a52f2` (fix)
2. **Task 2: Run existing test suite** - (no commit — no files modified; test run only)

**Plan metadata:** (docs commit to follow)

## Files Created/Modified
- `kbd_backlight/ui/application.py` - Moved `_restore_last_profile()` call from inside `if not self._tray_only:` to immediately before it; added inline PROF-04 comment

## Decisions Made
- Single call before the conditional is sufficient for both code paths — no need to duplicate the call inside the `if not self._tray_only:` branch
- Method body left unchanged: `persist=False` (cmd=0) and `try/except Exception: pass` are both correct and intentional

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

`pytest` not installed on system; fell back to `python3 -m unittest discover -s tests -v` as specified in the plan fallback. All 72 tests passed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- PROF-04 gap is now closed
- Plan 04.1-02 (PROF-02 gap closure) can proceed independently
- No blockers introduced by this change
- Hardware restore on autostart verified by code inspection; full integration verification happens at end of phase 04.1

## Self-Check: PASSED

All files and commits verified:
- `kbd_backlight/ui/application.py` — FOUND
- `04.1-01-SUMMARY.md` — FOUND
- Commit `d2a52f2` — FOUND

---
*Phase: 04.1-profile-gap-closure*
*Completed: 2026-02-23*
